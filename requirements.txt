# UI 前端框架
# v0.17 · 升到 1.36+:st.navigation() stable，啟用 sidebar 分組導航
streamlit>=1.36.0

# LLM API 溝通 (用來打 vLLM 的 OpenAI 相容 API)
openai>=1.10.0

# 資料處理與科學運算
pandas>=2.0.0
numpy>=1.24.0

# 互動式視覺化套件
plotly>=5.18.0
# ECharts (透過 streamlit component 渲染,提供 BI 風格動畫與互動)
streamlit-echarts>=0.4.0

# MongoDB 連線驅動程式
pymongo>=4.6.0

# Phase D insight 用 DataFrame.to_markdown() 預覽 Q 表
tabulate>=0.9.0

# v0.3.0+: Prompt / metadata 從 MongoDB 讀取的 template 渲染引擎
jinja2>=3.1.0

# v0.3.2+: 載入 .env 檔案到 os.environ
python-dotenv>=1.0.0

# v0.4.0+: Export Insight → PPTX(matplotlib 已隨 pandas 進來,毋須額外列)
python-pptx>=0.6.21
matplotlib>=3.5.0

# v0.12.0+: Upload Workspace — parquet staging engine
pyarrow>=14.0.0
# Upload Workspace — Excel single sheet parser
openpyxl>=3.1.0

# v0.15.0+ (M5.5): DuckDB Phase A engine(大檔 >100K row 走 SQL path)
duckdb>=1.0.0

# v0.16.0+ (Sprint 1-3): RAG dynamic prompt
# sentence-transformers all-MiniLM-L6-v2 是預設 embedder(~90MB,CPU-only)
sentence-transformers>=2.5.0
# Chroma embedded mode(無 server,純 file-based vector store)
chromadb>=0.4.22
# 直接 import 用(用於 air-gap 模型下載 huggingface-cli)
huggingface_hub>=0.20.0