# UI 前端框架
streamlit>=1.30.0

# LLM API 溝通 (用來打 vLLM 的 OpenAI 相容 API)
openai>=1.10.0

# 資料處理與科學運算
pandas>=2.0.0
numpy>=1.24.0

# 互動式視覺化套件
plotly>=5.18.0
# ECharts (透過 streamlit component 渲染,提供 BI 風格動畫與互動)
streamlit-echarts>=0.4.0

# MongoDB 連線驅動程式
pymongo>=4.6.0

# Phase D insight 用 DataFrame.to_markdown() 預覽 Q 表
tabulate>=0.9.0

# v0.3.0+: Prompt / metadata 從 MongoDB 讀取的 template 渲染引擎
jinja2>=3.1.0

# v0.3.2+: 載入 .env 檔案到 os.environ
python-dotenv>=1.0.0

# v0.4.0+: Export Insight → PPTX(matplotlib 已隨 pandas 進來,毋須額外列)
python-pptx>=0.6.21
matplotlib>=3.5.0

# v0.12.0+: Upload Workspace — parquet staging engine
pyarrow>=14.0.0
# Upload Workspace — Excel single sheet parser
openpyxl>=3.1.0